export { default as Fulltext } from "./Fulltext";

// export const escapeFulltext = (fulltext: string): string => {
//   let value: string = unescapeFulltext(fulltext);
//   if (fulltext) {
//     value = value.replace(/(\")/g, '\\"');
//   }
//   return value;
// };

// export const unescapeFulltext = (fulltext: string): string => {
//   let value: string = "";
//   if (fulltext) {
//     value = fulltext.replace(/\\/g, "");
//   }
//   return value;
// };
